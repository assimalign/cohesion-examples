<Project>
  <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../'))" />
  <PropertyGroup>
    <CohesionApplication>appc</CohesionApplication>   <!-- the realization boundary for every resource in this folder -->
  </PropertyGroup>
</Project>
