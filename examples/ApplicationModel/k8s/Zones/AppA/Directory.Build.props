<Project>
  <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../'))" />
  <PropertyGroup>
    <CohesionApplication>appa</CohesionApplication>   <!-- the realization boundary for every resource in this folder -->
  </PropertyGroup>
</Project>
