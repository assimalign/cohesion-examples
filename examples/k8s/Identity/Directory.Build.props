<Project>
  <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../'))" />
  <PropertyGroup>
    <CohesionApplication>identity</CohesionApplication>   <!-- the realization boundary for every resource in this folder -->
  </PropertyGroup>
</Project>
